# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# netlify deploy artifacts
/netlify-deploy/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — DO NOT commit secrets
.env
.env.local
.env*.local

# .env.production is safe to commit (no real secrets, just placeholders)
# Real secrets go in Netlify dashboard → Environment Variables

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local data (SQLite, not needed in repo)
/data/

# checkpoints (large snapshots, not needed in repo)
/checkpoints/
